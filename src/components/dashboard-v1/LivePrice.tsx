import React from 'react';

type Props = { symbol: string };

export default function LivePrice(_props: Props) {
  // Minimal client placeholder used for typechecking and dev fallback
  return <span aria-hidden />;
}
