"use client";
import React from 'react';

export default function MarketPollingClient() {
  // Minimal polling client placeholder — no runtime side effects
  React.useEffect(() => {
    return () => {};
  }, []);
  return null;
}
